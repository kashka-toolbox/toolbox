export type TypeDefinition = {
    /**
     * Technical name of the type
     *
     * This identifies the type and must be unique.
     */
    name: string;
    /**
     * Translation key for the type name
     */
    translationKey: string;
};